version: 2
jobs:
  build:
    docker:
    - image: circleci/node:7.10
    steps:
    - checkout
    - run: |
        git clone https://github.com/Hygieia/Hygieia.git
        cd Hygieia
        ls
        mvn -v
        docker -v
workflows:
  version: 2
  workflow:
    jobs:
    - build:
       filters:
            branches:
              only: master
